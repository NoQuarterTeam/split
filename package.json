{
  "name": "root",
  "private": true,
  "version": "1.0.0",
  "license": "MIT",
  "workspaces": {
    "packages": [
      "packages/*"
    ]
  },
  "scripts": {
    "build:api": "lerna run build --scope={@split/connector,@split/api}",
    "build:web": "lerna run build --scope={@split/connector,@split/web}",
    "test": "lerna run test",
    "lint": "lerna run lint",
    "pre-commit": "lerna run pre-commit",
    "heroku-postbuild": "yarn build:api"
  },
  "husky": {
    "hooks": {
      "pre-commit": "lerna run --concurrency 1 --stream pre-commit"
    }
  },
  "resolutions": {
    "graphql": "14.1.1"
  },
  "devDependencies": {
    "husky": "^1.3.1",
    "lerna": "^3.13.0",
    "lint-staged": "^8.1.5"
  }
}
