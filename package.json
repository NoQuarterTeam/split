{
  "name": "root",
  "private": true,
  "version": "1.0.0",
  "license": "MIT",
  "workspaces": {
    "packages": [
      "packages/*"
    ]
  },
  "scripts": {
    "build:api": "lerna run build --scope={@cg/connector,@cg/api}",
    "build:web": "lerna run build --scope={@cg/connector,@cg/web}",
    "lint": "lerna run lint",
    "heroku-postbuild": "yarn build:api"
  },
  "resolutions": {
    "graphql": "14.1.1"
  },
  "devDependencies": {
    "lerna": "^3.13.0"
  }
}
