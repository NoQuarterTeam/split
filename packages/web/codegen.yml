schema: http://localhost:5000/graphql
documents: ./src/lib/graphql/**/*.ts
overwrite: true
generates:
  ./src/lib/graphql/types.ts:
    plugins:
      - typescript-common
      - typescript-client
      - typescript-server
